# Local PostgreSQL connection. Create the database before applying src/schema.sql.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/workout_planner

# Set to true for hosting providers that require TLS for PostgreSQL connections.
DATABASE_SSL=false

PORT=5000
